function move(position, roll) {
  // return the new position

  return position + roll * 2;

  //   let currentPosition;
  //   if (position === 0) {
  //     currentPosition = 0;
  //     currentPosition += roll * 2;
  //     console.log(currentPosition);
  //   } else {
  //     currentPosition = position;
  //     currentPosition += roll * 2;
  //     console.log(currentPosition);
  //   }
}

move(2, 4);
